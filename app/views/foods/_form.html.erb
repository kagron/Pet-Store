<%= form_with(model: food, local: true, html: { multipart: true }) do |form| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.file_field :avatar %>
  </div>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :food_name %>
  </div>

  <div class="field">
    <%= form.label :expiration %>
    <%= form.date_select :expirationDate, id: :food_expiration %>
  </div>

  <div class="field">
    <%= form.label :data %>
    <%= form.date_select :data, id: :food_data %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :food_price %>
  </div>

  <div class="field">
    <%= form.label :foodType %>
    <%= form.text_field :foodType, id: :food_foodType %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, id: :food_quantity %>
  </div>
  <div class="field">
    <%= form.label :store_id %>
    <%= form.number_field :store_id, id: :store_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
