<%= form_with(model: toy, local: true) do |form| %>
  <% if toy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(toy.errors.count, "error") %> prohibited this toy from being saved:</h2>

      <ul>
      <% toy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand, id: :toy_brand %>
  </div>

  <div class="field">
    <%= form.label :toyType %>
    <%= form.text_field :toyType, id: :toy_type %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :toy_quantity %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :toy_description %>
  </div>

  <div class="field">
    <%= form.label :store_id %>
    <%= form.number_field :store_id, id: :store_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
